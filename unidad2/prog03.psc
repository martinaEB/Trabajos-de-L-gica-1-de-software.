Algoritmo prog03
	//pedimos un numero al usuario.
	escribir'Ingrese un numero: '
	//lo almacenamos en un espacio de memoria llamada a.
	leer a
	// pedimos otro numero al usuario.
	escribir 'Ingrese otro numero: '
	//lo almacenamos en un espacio llamaddo b.
	leer b
	//guardamos en otro de memoria llamado c, la suma de a+b
	c <- a+b
	//mostramos el resultado en patalla.
	escribir'la suma de los numeros da ', C
FinAlgoritmo